<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BestDay UI</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      background: black;
      width: 100vw;
      height: 100vh;
      overflow: hidden;
    }
    #login-screen, #desktop-screen {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }
    #login-screen img, #desktop-screen img {
      display: block;
      max-width: 100%;
    }
    #login-ui {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    #login-ui input {
      padding: 10px;
      font-size: 1rem;
      width: 200px;
    }
    #login-ui button {
      margin-top: 10px;
      padding: 10px 20px;
    }
    #error-msg {
      color: red;
      font-family: sans-serif;
      margin-bottom: 10px;
      display: none;
    }
    #clock {
      position: absolute;
      bottom: 40px;
      right: 50px;
      font-family: monospace;
      font-size: 18px;
      color: black;
    }
    #exit-button {
      position: absolute;
      bottom: 40px;
      left: 50px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div id="login-screen">
    <img src="LogIN.png" alt="Login Background">
    <div id="login-ui">
      <div id="error-msg">Ошибка кода</div>
      <input type="text" id="password" placeholder="Введите код">
      <button onclick="checkPassword()">Send</button>
    </div>
  </div>

  <div id="desktop-screen" style="display:none;">
    <img src="LogIN (2).png" alt="Desktop Background">
    <div id="clock"></div>
    <img id="exit-button" src="off.png" alt="Exit" width="48" onclick="window.close()">
  </div>

  <script>
    function checkPassword() {
      const input = document.getElementById("password").value;
      const error = document.getElementById("error-msg");
      if (input === "682") {
        document.getElementById("login-screen").style.display = "none";
        document.getElementById("desktop-screen").style.display = "block";
      } else {
        error.style.display = "block";
      }
    }

    function updateTime() {
      const now = new Date();
      const hh = now.getHours().toString().padStart(2, '0');
      const mm = now.getMinutes().toString().padStart(2, '0');
      document.getElementById("clock").textContent = `${hh}:${mm}`;
    }
    setInterval(updateTime, 1000);
    updateTime();
  </script>
</body>
</html>
